<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             x:Class="NG.MicroERP.App.Pages.KitchenOrderPage"
             Title="KitchenOrderPage">

    <ContentPage.Background>
        <LinearGradientBrush EndPoint="0,1">
            <GradientStop Color="#E6F0FA" Offset="0.0" />
            <GradientStop Color="#FFFFFF" Offset="1.0" />
        </LinearGradientBrush>
    </ContentPage.Background>
    
    
    <ScrollView>
        <VerticalStackLayout Padding="15" Spacing="15">

            <!-- Order Info -->
            <Border StrokeThickness="0"
                    Margin="10"
                    Padding="20"
                    BackgroundColor="White"
                    HorizontalOptions="Fill"
                    StrokeShape="RoundRectangle 15">
                
                <Border.Shadow>
                    <Shadow Brush="#20000000" Offset="0,5" Radius="10" Opacity="0.3" />
                </Border.Shadow>
                
                <VerticalStackLayout Spacing="5">
                    <Label Text="Order Information" FontSize="20" FontAttributes="Bold"/>
                    <Label Text="{Binding Bill.SeqNo}" FontSize="18" TextColor="DarkBlue"/>

                    <Label Text="{Binding Bill.TranDate, StringFormat='Time: {0:HH:mm dd/MM/yyyy}'}"/>
                    <Label Text="{Binding Bill.TableName, StringFormat='Table: {0}'}"/>
                    <Label Text="{Binding Bill.Fullname, StringFormat='Server: {0}'}"/>
                    <Label Text="{Binding Bill.Location, StringFormat='Location: {0}'}"/>
                </VerticalStackLayout>
            </Border>

            <!-- Special Instructions -->
            <Border StrokeThickness="0"
                    Margin="10"
                    Padding="20"
                    BackgroundColor="White"
                    HorizontalOptions="Fill"
                    StrokeShape="RoundRectangle 15">
                <Border.Shadow>
                    <Shadow Brush="#20000000" Offset="0,5" Radius="10" Opacity="0.3" />
                </Border.Shadow>
                <VerticalStackLayout>
                    <Label Text="Special Instructions:" FontAttributes="Bold"/>
                    <Label Text="{Binding Bill.Description}" FontSize="16" TextColor="Black" LineBreakMode="WordWrap"/>
                </VerticalStackLayout>
            </Border>

            <!-- Item List -->
            <Label Text="Order Items" FontSize="20" FontAttributes="Bold" />

            <CollectionView x:Name="ItemsList" SelectionMode="None">
                <CollectionView.ItemTemplate>
                    <DataTemplate>
                        <Border StrokeThickness="0"
                                Margin="10"
                                Padding="20"
                                BackgroundColor="White"
                                HorizontalOptions="Fill"
                                StrokeShape="RoundRectangle 15">
                            <Border.Shadow>
                                <Shadow Brush="#20000000" Offset="0,5" Radius="10" Opacity="0.3" />
                            </Border.Shadow>
                            <VerticalStackLayout>
                                <HorizontalStackLayout Spacing="10">
                                    <Label Text="{Binding Qty}" FontSize="18" FontAttributes="Bold" TextColor="DarkRed"/>
                                    <Label Text="{Binding Item}" FontSize="18"/>
                                </HorizontalStackLayout>
                                <Label Text="{Binding ServingSize}" FontSize="14" TextColor="Gray"/>
                                <Label Text="{Binding Description}" FontSize="14" TextColor="DimGray"/>
                            </VerticalStackLayout>
                        </Border>
                    </DataTemplate>
                </CollectionView.ItemTemplate>
            </CollectionView>

        </VerticalStackLayout>
    </ScrollView>
</ContentPage>