@using MudBlazor

<MudDrawer @bind-Open="Open"
           Width="900px"
           Anchor="Anchor.Right"
           Elevation="3"
           Variant="DrawerVariant.Temporary"
           Class="professional-drawer">

    <!-- Clean Professional Header -->
    <div class="drawer-header">
        <div class="d-flex align-center flex-grow-1">
            @if (!string.IsNullOrEmpty(FaIcon))
            {
                <div class="icon-container me-3">
                    <i class="@FaIcon"></i>
                </div>
            }
            <MudText Typo="Typo.h5" Class="drawer-title flex-grow-1 mb-0">@Title</MudText>
        </div>
        <MudIconButton Icon="@Icons.Material.Filled.Close"
                       OnClick="CloseDrawerAsync"
                       Size="Size.Small" />
    </div>

    <!-- Content Area -->
    <div class="drawer-content">
        @ChildContent
    </div>

    <!-- Footer -->
    <div class="drawer-footer">
        @FooterContent
    </div>
</MudDrawer>


@code {
    [Parameter] public bool Open { get; set; } = false;
    [Parameter] public EventCallback<bool> OpenChanged { get; set; }
    [Parameter] public string? Title { get; set; } = string.Empty;
    [Parameter] public string? FaIcon { get; set; } = string.Empty;
    [Parameter] public RenderFragment HeaderContent { get; set; } = null!;
    [Parameter] public RenderFragment ChildContent { get; set; } = null!;
    [Parameter] public RenderFragment FooterContent { get; set; } = null!;
    private bool _hasRenderHandle = false;
    [Parameter] public EventCallback OnClose { get; set; }


    protected override async Task OnInitializedAsync()
    {
        Console.WriteLine("Component Initialized");
        await Task.Delay(50);
    }


    protected override async Task OnAfterRenderAsync(bool firstRender)
    {
        if (firstRender)
        {
            await Task.Delay(50);
            await InvokeAsync(StateHasChanged); // Ensures UI updates correctly
        }
    }


    public async Task OpenDrawer()
    {
        Open = true;
        await Task.Delay(50);
        await OpenChanged.InvokeAsync(Open);
        //await InvokeAsync(StateHasChanged); // Ensures UI updates correctly
    }

    public async Task CloseDrawerAsync()
    {
        try
        {
            Open = false;
            await Task.Delay(50);
            await OpenChanged.InvokeAsync(Open);
            if (_hasRenderHandle)
            {
                await InvokeAsync(StateHasChanged);
            }

        }
        catch (Exception ex)
        {

            Console.WriteLine($"CloseDrawer exception: {ex.Message}");
        }
    }


    protected override void OnAfterRender(bool firstRender)
    {
        if (firstRender)
        {
            _hasRenderHandle = true;
        }
    }


    public async Task OpenDrawerAsync()
    {
        await CloseDrawerAsync();
        await Task.Delay(50);
        await OpenDrawer();
    }
}


<style>

    /* DRAWER HEADER */
    .professional-drawer .drawer-header {
        display: flex;
        align-items: center;
        justify-content: space-between;
        padding: 1rem 1.5rem;
        border-bottom: 1px solid rgba(0, 0, 0, 0.1);
        background-color: rgba(255, 255, 255, 0.03); /* subtle overlay */
        backdrop-filter: blur(6px);
    }

    /* Dark mode header */
    body.mud-theme-dark .professional-drawer .drawer-header {
        border-bottom: 1px solid rgba(255, 255, 255, 0.15);
        background-color: rgba(0, 0, 0, 0.2);
    }

    /* ICON CONTAINER */
    .professional-drawer .icon-container {
        padding: 8px;
        border-radius: 10px;
        display: flex;
        align-items: center;
        justify-content: center;
        background-color: rgba(0,0,0,0.05); /* subtle for light mode */
        transition: all 0.2s ease;
    }

    body.mud-theme-dark .professional-drawer .icon-container {
        background-color: rgba(255,255,255,0.08); /* subtle for dark mode */
    }

    .professional-drawer .icon-container i {
        font-size: 1.6rem;
        color: var(--mud-palette-primary);
    }

    body.mud-theme-dark .professional-drawer .icon-container i {
        color: var(--mud-palette-primary-light);
    }

    /* TITLE TEXT */
    .professional-drawer .drawer-title {
        font-weight: 600;
        font-size: 1.3rem;
        color: var(--mud-palette-text-primary);
    }

    body.mud-theme-dark .professional-drawer .drawer-title {
        color: #ffffff;
        text-shadow: 0 1px 2px rgba(0,0,0,0.5);
    }

    /* CLOSE BUTTON */
    .professional-drawer .close-btn {
        color: var(--mud-palette-text-primary);
        transition: all 0.2s ease;
    }

    body.mud-theme-dark .professional-drawer .close-btn {
        color: #ffffff;
    }

    .professional-drawer .close-btn:hover {
        transform: scale(1.1);
        color: var(--mud-palette-error);
    }

</style>