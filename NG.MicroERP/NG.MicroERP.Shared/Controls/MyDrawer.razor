<MudDrawer @bind-Open="Open"
           Width="@Width"
           Anchor="Anchor.Right"
           Elevation="3"
           Variant="DrawerVariant.Temporary"
           Class="professional-drawer">

    <!-- Clean Professional Header with Gradient Theme -->
    <MudPaper Elevation="1" Class="drawer-header-paper" Stretch="true">
        <div class="drawer-header">
            <div class="d-flex align-center flex-grow-1">
                @if (!string.IsNullOrEmpty(FaIcon))
                {
                    <div class="icon-container me-3">
                        <i class="@FaIcon"></i>
                    </div>
                }
                <MudText Typo="Typo.h5" Class="drawer-title flex-grow-1 mb-0" Style="color: white !important;">@Title</MudText>
            </div>
            <MudIconButton Icon="@Icons.Material.Filled.Close"
                           OnClick="CloseDrawerAsync"
                           Size="Size.Small"
                           Style="color: white !important;" />
        </div>
    </MudPaper>

    <!-- Content Area -->
    <div class="drawer-content">
        @ChildContent
    </div>

    <!-- Footer -->
    <div class="drawer-footer">
        @FooterContent
    </div>
</MudDrawer>

@code {
    [Parameter] public bool Open { get; set; } = false;
    [Parameter] public EventCallback<bool> OpenChanged { get; set; }
    [Parameter] public string? Title { get; set; } = string.Empty;
    [Parameter] public string? FaIcon { get; set; } = string.Empty;
    [Parameter] public string Width { get; set; } = "1000px";
    [Parameter] public RenderFragment HeaderContent { get; set; } = null!;
    [Parameter] public RenderFragment ChildContent { get; set; } = null!;
    [Parameter] public RenderFragment FooterContent { get; set; } = null!;
    private bool _hasRenderHandle = false;
    [Parameter] public EventCallback OnClose { get; set; }

    protected override async Task OnInitializedAsync()
    {
        Console.WriteLine("Component Initialized");
        await Task.Delay(50);
    }

    protected override async Task OnAfterRenderAsync(bool firstRender)
    {
        if (firstRender)
        {
            await Task.Delay(50);
            await InvokeAsync(StateHasChanged);
        }
    }

    public async Task OpenDrawer()
    {
        Open = true;
        await Task.Delay(50);
        await OpenChanged.InvokeAsync(Open);
    }

    public async Task CloseDrawerAsync()
    {
        try
        {
            Open = false;
            await Task.Delay(50);
            await OpenChanged.InvokeAsync(Open);
            if (_hasRenderHandle)
            {
                await InvokeAsync(StateHasChanged);
            }
        }
        catch (Exception ex)
        {
            Console.WriteLine($"CloseDrawer exception: {ex.Message}");
        }
    }

    protected override void OnAfterRender(bool firstRender)
    {
        if (firstRender)
        {
            _hasRenderHandle = true;
        }
    }

    public async Task OpenDrawerAsync()
    {
        await CloseDrawerAsync();
        await Task.Delay(50);
        await OpenDrawer();
    }
}

<style>
    /* DRAWER HEADER PAPER - Gradient theme matching dashboard headers */
    .professional-drawer .drawer-header-paper {
        background: linear-gradient(135deg, var(--mud-palette-primary-lighten) 0%, var(--mud-palette-primary) 100%) !important;
        color: white !important;
        border-radius: 0 !important;
        margin: 0 !important;
        padding: 0 !important;
    }

    /* Dark mode gradient */
    .mud-theme-dark .professional-drawer .drawer-header-paper {
        background: linear-gradient(135deg, var(--mud-palette-primary-darken) 0%, var(--mud-palette-primary) 100%) !important;
    }

    /* DRAWER HEADER */
    .professional-drawer .drawer-header {
        display: flex;
        align-items: center;
        justify-content: space-between;
        padding: 1rem 1.5rem;
        border-bottom: 1px solid rgba(255, 255, 255, 0.1);
        background-color: transparent;
        width: 100%;
        box-sizing: border-box;
    }

    /* ICON CONTAINER */
    .professional-drawer .icon-container {
        padding: 8px;
        border-radius: 10px;
        display: flex;
        align-items: center;
        justify-content: center;
        background-color: rgba(255, 255, 255, 0.15);
        transition: all 0.2s ease;
    }

        .professional-drawer .icon-container:hover {
            background-color: rgba(255, 255, 255, 0.25);
            transform: scale(1.05);
        }

        .professional-drawer .icon-container i {
            font-size: 1.6rem;
            color: white;
        }

    /* TITLE TEXT */
    .professional-drawer .drawer-title {
        font-weight: 600;
        font-size: 1.3rem;
        color: white !important;
        text-shadow: 0 1px 2px rgba(0, 0, 0, 0.2);
    }

    /* CLOSE BUTTON */
    .professional-drawer .drawer-header .mud-icon-button {
        color: white !important;
        transition: all 0.2s ease;
    }

        .professional-drawer .drawer-header .mud-icon-button:hover {
            transform: scale(1.1);
            background-color: rgba(255, 255, 255, 0.2) !important;
            color: white !important;
        }

    /* CONTENT AREA */
    .professional-drawer .drawer-content {
        padding: 1.5rem;
        flex-grow: 1;
        overflow-y: auto;
    }

    /* FOOTER AREA */
    .professional-drawer .drawer-footer {
        padding: 1rem 1.5rem;
        border-top: 1px solid var(--mud-palette-divider);
        background-color: var(--mud-palette-background);
    }

    /* Dark theme adjustments */
    body.mud-theme-dark .professional-drawer .drawer-footer {
        background-color: var(--mud-palette-surface);
        border-top: 1px solid var(--mud-palette-divider);
    }
</style>