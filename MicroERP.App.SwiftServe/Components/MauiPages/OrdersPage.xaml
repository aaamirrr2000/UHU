<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             xmlns:converters="clr-namespace:MicroERP.App.SwiftServe.Components.MauiPages.Converters"
             xmlns:controls="clr-namespace:MicroERP.App.SwiftServe.Components.MauiPages.Controls"
             x:Class="MicroERP.App.SwiftServe.Components.MauiPages.OrdersPage"
             Title="Orders"
             BackgroundColor="#F0F0F0">

    <ContentPage.Resources>
        <ResourceDictionary>
            <Color x:Key="WhiteColor">#FFFFFF</Color>
            <Color x:Key="TextPrimary">#212121</Color>
            <Color x:Key="TextSecondary">#666666</Color>
            <Color x:Key="BorderColor">#E0E0E0</Color>
            <Color x:Key="GreenAccent">#2E7D32</Color>
            <Color x:Key="DangerColor">#dc3545</Color>
            <Color x:Key="InfoColor">#17a2b8</Color>
            <converters:IsNotEmptyConverter x:Key="IsNotEmptyConverter"/>
        </ResourceDictionary>
    </ContentPage.Resources>

    <Grid RowDefinitions="Auto,*">
        <controls:NavigationMenu x:Name="NavMenu" Grid.Row="0" Grid.RowSpan="2" ZIndex="1000"/>

        <ScrollView Grid.Row="1">
            <StackLayout Spacing="0" Padding="16" Margin="0,0,0,24">
                <!-- Header: Back | Orders | Close (same as Cart) -->
                <Grid ColumnDefinitions="Auto,*,Auto" Margin="0,8,0,20" ColumnSpacing="8">
                    <Button Text="&lt;" Clicked="OnBackClicked" BackgroundColor="Transparent" TextColor="{StaticResource TextPrimary}"
                            FontSize="24" WidthRequest="44" HeightRequest="44" VerticalOptions="Center" Grid.Column="0"/>
                    <Label x:Name="HeaderTitle" Text="Orders" FontSize="22" FontAttributes="Bold" TextColor="{StaticResource TextPrimary}"
                           HorizontalOptions="Center" VerticalOptions="Center" Grid.Column="1"/>
                    <Button Text="✕" Clicked="OnBackClicked" BackgroundColor="Transparent" TextColor="{StaticResource TextPrimary}"
                            FontSize="22" WidthRequest="44" HeightRequest="44" VerticalOptions="Center" Grid.Column="2"/>
                </Grid>

                <!-- Filters (Cart-style white card) -->
                <Frame BackgroundColor="{StaticResource WhiteColor}" BorderColor="{StaticResource BorderColor}"
                       CornerRadius="12" Padding="14" Margin="0,0,0,16" HasShadow="True">
                    <Grid ColumnDefinitions="*,*,Auto" ColumnSpacing="10" RowDefinitions="Auto,Auto" RowSpacing="10">
                        <Label Text="Date" FontSize="13" FontAttributes="Bold" TextColor="{StaticResource TextSecondary}" Grid.Row="0" Grid.Column="0"/>
                        <Label Text="Search" FontSize="13" FontAttributes="Bold" TextColor="{StaticResource TextSecondary}" Grid.Row="0" Grid.Column="1"/>
                        <DatePicker x:Name="DatePicker" DateSelected="OnDateChanged" Date="{Binding FromDate}"
                                    Format="yyyy-MM-dd" Grid.Row="1" Grid.Column="0"/>
                        <Frame Grid.Row="1" Grid.Column="1" BackgroundColor="#F5F5F5" BorderColor="{StaticResource BorderColor}"
                               CornerRadius="10" Padding="10" HasShadow="False">
                            <Entry x:Name="SearchEntry" Placeholder="Search" TextChanged="OnSearchTextChanged"
                                   BackgroundColor="Transparent" TextColor="{StaticResource TextPrimary}" PlaceholderColor="{StaticResource TextSecondary}"/>
                        </Frame>
                        <Button Text="↻" Clicked="OnRefreshClicked" BackgroundColor="{StaticResource GreenAccent}"
                                TextColor="{StaticResource WhiteColor}" CornerRadius="10" WidthRequest="48" HeightRequest="48"
                                FontSize="20" Grid.Row="0" Grid.Column="2" Grid.RowSpan="2" VerticalOptions="Center"/>
                    </Grid>
                </Frame>

                <!-- Orders list (white cards like Cart) -->
                <CollectionView x:Name="OrdersCollectionView" ItemsSource="{Binding FilteredInvoices}"
                                SelectionMode="None" Margin="0,0,0,20">
                    <CollectionView.ItemTemplate>
                        <DataTemplate>
                            <Frame BackgroundColor="{StaticResource WhiteColor}" BorderColor="{StaticResource BorderColor}"
                                   CornerRadius="12" Padding="14" HasShadow="True" Margin="0,0,0,12">
                                <StackLayout Spacing="12">
                                    <Grid ColumnDefinitions="*,Auto" ColumnSpacing="8">
                                        <StackLayout Grid.Column="0" Spacing="4">
                                            <Label Text="{Binding Code, StringFormat='Bill: {0}'}" FontSize="16" FontAttributes="Bold"
                                                   TextColor="{StaticResource TextPrimary}"/>
                                            <Label Text="{Binding TableName, StringFormat='Table: {0}'}" FontSize="14"
                                                   TextColor="{StaticResource TextSecondary}"/>
                                            <Label Text="{Binding ServiceType, StringFormat='Service: {0}'}" FontSize="14"
                                                   TextColor="{StaticResource TextSecondary}"/>
                                            <Label Text="{Binding PartyName, StringFormat='Customer: {0}'}" FontSize="14"
                                                   TextColor="{StaticResource TextSecondary}"
                                                   IsVisible="{Binding PartyName, Converter={StaticResource IsNotEmptyConverter}}"/>
                                        </StackLayout>
                                        <Border BackgroundColor="{StaticResource GreenAccent}" StrokeThickness="0" Padding="10,6"
                                                VerticalOptions="Start" Grid.Column="1">
                                            <Border.StrokeShape><RoundRectangle CornerRadius="8"/></Border.StrokeShape>
                                            <Label Text="{Binding Status}" FontSize="12" FontAttributes="Bold" TextColor="{StaticResource WhiteColor}"/>
                                        </Border>
                                    </Grid>
                                    <Label x:Name="WaitingTimeLabel" Text="{Binding ElapsedTime, StringFormat='Waiting: {0}'}"
                                           FontSize="14" FontAttributes="Bold" TextColor="{StaticResource DangerColor}" IsVisible="False"/>
                                    <StackLayout Orientation="Horizontal" Spacing="8">
                                        <Button Text="View" Clicked="OnViewClicked" CommandParameter="{Binding .}"
                                                BackgroundColor="{StaticResource GreenAccent}" TextColor="{StaticResource WhiteColor}"
                                                CornerRadius="10" Padding="14,10" FontSize="14"/>
                                        <Button x:Name="DeleteButton" Text="Delete" Clicked="OnDeleteClicked" CommandParameter="{Binding .}"
                                                BackgroundColor="{StaticResource DangerColor}" TextColor="{StaticResource WhiteColor}"
                                                CornerRadius="10" Padding="14,10" FontSize="14" IsVisible="False"/>
                                        <Button x:Name="GenerateBillButton" Text="Gen. Bill" Clicked="OnGenerateBillClicked" CommandParameter="{Binding .}"
                                                BackgroundColor="{StaticResource GreenAccent}" TextColor="{StaticResource WhiteColor}"
                                                CornerRadius="10" Padding="14,10" FontSize="14" IsVisible="False"/>
                                        <Button x:Name="NotesButton" Text="Notes" Clicked="OnNotesClicked" CommandParameter="{Binding .}"
                                                BackgroundColor="{StaticResource InfoColor}" TextColor="{StaticResource WhiteColor}"
                                                CornerRadius="10" Padding="14,10" FontSize="14" IsVisible="False"/>
                                        <Button x:Name="ReadyButton" Text="Ready" Clicked="OnReadyClicked" CommandParameter="{Binding .}"
                                                BackgroundColor="{StaticResource InfoColor}" TextColor="{StaticResource WhiteColor}"
                                                CornerRadius="10" Padding="14,10" FontSize="14" IsVisible="False"/>
                                    </StackLayout>
                                </StackLayout>
                            </Frame>
                        </DataTemplate>
                    </CollectionView.ItemTemplate>
                </CollectionView>

                <StackLayout x:Name="EmptyStateLayout" IsVisible="False" Spacing="16"
                             HorizontalOptions="Center" VerticalOptions="Center" Margin="0,40">
                    <Label Text="📋" FontSize="72" HorizontalOptions="Center" Opacity="0.4"/>
                    <Label Text="No orders found" FontSize="20" FontAttributes="Bold" TextColor="{StaticResource TextSecondary}" HorizontalOptions="Center"/>
                </StackLayout>
            </StackLayout>
        </ScrollView>

        <controls:LoadingOverlay x:Name="LoadingOverlay" Grid.Row="0" Grid.RowSpan="2" IsVisible="False" Message="Loading orders..."/>
    </Grid>
</ContentPage>

