<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             xmlns:controls="clr-namespace:MicroERP.App.SwiftServe.Components.MauiPages.Controls"
             xmlns:converters="clr-namespace:MicroERP.App.SwiftServe.Components.MauiPages.Converters"
             x:Class="MicroERP.App.SwiftServe.Components.MauiPages.KitchenPage"
             Title="Kitchen"
             BackgroundColor="#F5F5F5">

    <ContentPage.Resources>
        <ResourceDictionary>
            <Color x:Key="PrimaryColor">#512BD4</Color>
            <Color x:Key="WhiteColor">#FFFFFF</Color>
            <Color x:Key="TextPrimary">#212121</Color>
            <Color x:Key="TextSecondary">#666666</Color>
            <Color x:Key="SuccessColor">#28a745</Color>
            <Color x:Key="DangerColor">#dc3545</Color>
            <Color x:Key="WarningColor">#ffc107</Color>
            <Color x:Key="InfoColor">#17a2b8</Color>
            <Color x:Key="BorderColor">#E0E0E0</Color>
            <converters:IsNotEmptyConverter x:Key="IsNotEmptyConverter"/>
        </ResourceDictionary>
    </ContentPage.Resources>

    <Grid RowDefinitions="*">
        <controls:NavigationMenu x:Name="NavMenu" Grid.Row="0" Grid.RowSpan="2" ZIndex="1000"/>

        <ScrollView Grid.Row="0">
            <StackLayout Spacing="12" Padding="12" Margin="0,0,0,24">
                <Grid ColumnDefinitions="Auto,*,Auto" Margin="0,8,0,16" ColumnSpacing="8">
                    <Button Text="← Back" Clicked="OnBackClicked" BackgroundColor="Transparent"
                            TextColor="{StaticResource PrimaryColor}" FontSize="16" Padding="12,8" Grid.Column="0"/>
                    <Label Text="Kitchen Orders" FontSize="22" FontAttributes="Bold" TextColor="{StaticResource TextPrimary}"
                           HorizontalOptions="Center" VerticalOptions="Center" Grid.Column="1"/>
                    <Button Text="↻" Clicked="OnRefreshClicked" BackgroundColor="{StaticResource PrimaryColor}"
                            TextColor="{StaticResource WhiteColor}" FontSize="18" WidthRequest="44" HeightRequest="44"
                            CornerRadius="8" Grid.Column="2"/>
                </Grid>

                <CollectionView x:Name="KitchenCollectionView" ItemsSource="{Binding OrderGroups}"
                               SelectionMode="None">
                    <CollectionView.ItemTemplate>
                        <DataTemplate>
                            <Frame BackgroundColor="{StaticResource WhiteColor}" BorderColor="{StaticResource BorderColor}"
                                   CornerRadius="12" Padding="14" HasShadow="True" Margin="0,0,0,12">
                                <StackLayout Spacing="12">
                                    <StackLayout Orientation="Horizontal" Spacing="8">
                                        <Label Text="{Binding SeqNo}" FontSize="16" FontAttributes="Bold"
                                               TextColor="{StaticResource PrimaryColor}"/>
                                        <Label Text="{Binding TableName, StringFormat='Table: {0}'}" FontSize="14"
                                               TextColor="{StaticResource TextSecondary}"/>
                                        <Label Text="{Binding PartyName, StringFormat=' · {0}'}" FontSize="14"
                                               TextColor="{StaticResource TextSecondary}"
                                               IsVisible="{Binding PartyName, Converter={StaticResource IsNotEmptyConverter}}"/>
                                    </StackLayout>
                                    <CollectionView ItemsSource="{Binding Items}" SelectionMode="None">
                                        <CollectionView.ItemTemplate>
                                            <DataTemplate>
                                                <Frame BackgroundColor="#FAFAFA" BorderColor="#EEEEEE"
                                                       CornerRadius="8" Padding="10" Margin="0,0,0,6" HasShadow="False">
                                                    <Grid ColumnDefinitions="*,Auto,Auto,Auto" ColumnSpacing="8">
                                                        <StackLayout Grid.Column="0" Spacing="2" VerticalOptions="Center">
                                                            <Label Text="{Binding Item}" FontSize="15" FontAttributes="Bold"
                                                                   TextColor="{StaticResource TextPrimary}"/>
                                                            <Label Text="{Binding Qty, StringFormat='Qty: {0}'}"
                                                                   FontSize="13" TextColor="{StaticResource TextSecondary}"/>
                                                        </StackLayout>
                                                        <Border Grid.Column="1" Padding="8,4" VerticalOptions="Center"
                                                                BackgroundColor="{StaticResource InfoColor}" StrokeThickness="0">
                                                            <Border.StrokeShape><RoundRectangle CornerRadius="6"/></Border.StrokeShape>
                                                            <Border.Triggers>
                                                                <DataTrigger TargetType="Border" Binding="{Binding InvoiceDetailStatus}" Value="COMPLETE">
                                                                    <Setter Property="BackgroundColor" Value="{StaticResource SuccessColor}"/>
                                                                </DataTrigger>
                                                                <DataTrigger TargetType="Border" Binding="{Binding InvoiceDetailStatus}" Value="IN_PROGRESS">
                                                                    <Setter Property="BackgroundColor" Value="{StaticResource WarningColor}"/>
                                                                </DataTrigger>
                                                            </Border.Triggers>
                                                            <Label Text="{Binding InvoiceDetailStatus}" FontSize="11" FontAttributes="Bold"
                                                                   TextColor="{StaticResource WhiteColor}"/>
                                                        </Border>
                                                        <Button Grid.Column="2" Text="In Progress" Clicked="OnMarkInProgress"
                                                                BackgroundColor="{StaticResource WarningColor}" TextColor="{StaticResource WhiteColor}"
                                                                FontSize="12" Padding="10,6" CornerRadius="6"
                                                                BindingContext="{Binding .}"/>
                                                        <Button Grid.Column="3" Text="Complete" Clicked="OnMarkComplete"
                                                                BackgroundColor="{StaticResource SuccessColor}" TextColor="{StaticResource WhiteColor}"
                                                                FontSize="12" Padding="10,6" CornerRadius="6"
                                                                BindingContext="{Binding .}"/>
                                                    </Grid>
                                                </Frame>
                                            </DataTemplate>
                                        </CollectionView.ItemTemplate>
                                    </CollectionView>
                                </StackLayout>
                            </Frame>
                        </DataTemplate>
                    </CollectionView.ItemTemplate>
                </CollectionView>

                <StackLayout x:Name="EmptyStateLayout" IsVisible="False" Spacing="16"
                             HorizontalOptions="Center" VerticalOptions="Center" Margin="0,40">
                    <Label Text="🍳" FontSize="72" HorizontalOptions="Center" Opacity="0.4"/>
                    <Label Text="No open orders" FontSize="18" FontAttributes="Bold"
                           TextColor="{StaticResource TextSecondary}" HorizontalOptions="Center"/>
                </StackLayout>
            </StackLayout>
        </ScrollView>

        <controls:LoadingOverlay x:Name="LoadingOverlay" Grid.Row="0" IsVisible="False" Message="Loading..."/>
    </Grid>
</ContentPage>
